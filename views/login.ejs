<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - BikeRental</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family:'Poppins', sans-serif; }
    body { background:#f4f4f9; color:#333; min-height:100vh; display:flex; flex-direction:column; }

    /* HEADER */
    header {
      background:#1e1e2f; color:#fff; padding:15px 6%;
      display:flex; justify-content:space-between; align-items:center;
      box-shadow:0 2px 8px rgba(0,0,0,0.2); position:relative;
    }
    header h1 { color:#ff6b6b; font-size:1.9rem; font-weight:600; }

    /* NAV LINKS */
    nav { display:flex; gap:22px; }
    nav a {
      color:#fff; text-decoration:none; padding:8px 18px;
      border-radius:6px; transition:.3s; font-weight:500;
    }
    nav a:hover { background:#ff6b6b; }

    /* HAMBURGER */
    #menu-toggle { display:none; }
    .menu-icon { display:none; font-size:32px; cursor:pointer; color:white; }

    /* LOGIN BOX */
    main.container {
      flex:1; display:flex; justify-content:center; align-items:center;
      padding:40px 5%;
    }
    .form-box {
      background:#fff; padding:45px 32px; border-radius:12px;
      width:100%; max-width:420px;
      box-shadow:0 4px 20px rgba(0,0,0,0.15); text-align:center;
    }
    .form-box h2 { margin-bottom:25px; font-size:1.9rem; }
    .form-box input {
      width:100%; padding:14px; margin-bottom:16px;
      border:1px solid #ccc; border-radius:6px; font-size:1rem;
    }
    .form-box button {
      width:100%; padding:12px; background:#ff6b6b; border:none; color:#fff;
      border-radius:6px; font-size:1rem; cursor:pointer; transition:.3s;
    }
    .form-box button:hover { background:#ff4b4b; }
    .form-box p { margin-top:15px; color:#555; }
    .form-box p a { color:#ff6b6b; text-decoration:none; font-weight:600; }

    .alert { background:#ffe4e4; color:#d8000c; padding:10px; border-radius:6px; margin-bottom:15px; }

    /* FOOTER */
    footer { background:#1e1e2f; color:#fff; padding:45px 6% 28px; }
    .footer-container {
      display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:35px;
    }
    footer h3 { color:#ff6b6b; margin-bottom:12px; font-size:1.2rem; }
    footer p, footer a { color:#e6e6e6; font-size:0.92rem; text-decoration:none; }
    footer a:hover { text-decoration:underline; }
    footer form input, footer form textarea {
      width:100%; padding:9px; margin-bottom:8px;
      border-radius:6px; border:1px solid #ccc;
    }
    footer button {
      background:#ff6b6b; color:#fff; padding:9px 14px;
      border:none; border-radius:6px; cursor:pointer; transition:.3s;
      font-size:0.9rem;
    }
    footer button:hover { background:#ff4b4b; }
    footer .copyright {
      text-align:center; margin-top:28px; color:#bbb; font-size:0.88rem;
    }

    /* RESPONSIVE */
    @media(max-width:850px){
      header h1 { font-size:1.7rem; }
    }
    @media(max-width:700px){
      .menu-icon { display:block; }
      nav {
        position:absolute; top:70px; left:0; width:100%;
        background:#1e1e2f; flex-direction:column;
        display:none; padding:20px 0; gap:15px;
      }
      nav a { width:100%; text-align:center; font-size:1.05rem; }
      #menu-toggle:checked ~ nav { display:flex; }
    }
    @media(max-width:540px){
      main.container { padding:20px 4%; }
      .form-box {
        padding:30px 22px;
        max-width:360px;
      }
      .form-box h2 { font-size:1.6rem; }
    }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <header>
    <h1>BikeRental</h1>
    <input type="checkbox" id="menu-toggle">
    <label for="menu-toggle" class="menu-icon">&#9776;</label>
    <nav>
      <a href="/">Home</a>
      <a href="/signup">Signup</a>
    </nav>
  </header>

  <!-- LOGIN FORM -->
  <main class="container">
    <div class="form-box">
      <h2>Login</h2>
      <% if (typeof message !== 'undefined' && message) { %>
        <div class="alert"><%= message %></div>
      <% } %>
      <form action="/login" method="POST">
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
      </form>
      <p>Don't have an account? <a href="/signup">Signup</a></p>
    </div>
  </main>

  <!-- FOOTER -->
  <footer>
    <div class="footer-container">
      <div>
        <h3>BikeRental</h3>
        <p>Your trusted bike rental service.</p>
      </div>
      <div>
        <h3>Links</h3>
        <p><a href="/">Home</a></p>
        <p><a href="/login">Login</a></p>
        <p><a href="/signup">Signup</a></p>
      </div>
      <div>
        <h3>Contact</h3>
        <p>Email: support@bikerental.com</p>
        <p>Phone: +91 9876543210</p>
        <p>Address: 123 Main St, City</p>
      </div>
      <div>
        <h3>Feedback</h3>
        <form action="/feedback" method="POST">
          <input type="text" name="name" placeholder="Your Name" required>
          <input type="email" name="email" placeholder="Email" required>
          <textarea name="message" placeholder="Your feedback" required></textarea>
          <button type="submit">Send</button>
        </form>
      </div>
    </div>
    <p class="copyright">© <%= new Date().getFullYear() %> BikeRental. All Rights Reserved.</p>
  </footer>

</body>
</html>
