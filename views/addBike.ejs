<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= bike ? "Edit Bike" : "Add Bike" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Arial', sans-serif; }

    body {
      background: #fff5f5; /* soft red shade like footer theme */
      color: #222;
    }

    /* Header */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 20px;
      background: #1b1b2f;
      color: #ff6b6b;
      flex-wrap: wrap;
    }
    header h1 { margin: 0; font-size: 24px; }
    header nav a {
      color: #fff;
      margin-left: 15px;
      text-decoration: none;
      font-weight: 500;
      transition: .3s ease;
    }
    header nav a:hover {
      color: #ff6b6b;
      transform: translateY(-2px);
    }

    /* Main container */
    .container {
      max-width: 520px;
      margin: 50px auto;
      padding: 20px;
    }

    /* Form Card */
    .form-box {
      background: #ffffff;
      padding: 32px 26px;
      border-radius: 12px;
      box-shadow: 0 8px 22px rgba(255, 95, 95, .25);
      border-top: 5px solid #ff6b6b;
    }
    .form-box h2 {
      text-align: center;
      margin-bottom: 25px;
      color: #1b1b2f;
      font-size: 26px;
    }
    .form-box label {
      font-weight: bold;
      color: #444;
      font-size: 15px;
      margin-top: 12px;
    }
    .form-box input,
    .form-box textarea {
      width: 100%;
      padding: 11px;
      margin: 8px 0 16px;
      border-radius: 6px;
      border: 1px solid #bbb;
      font-size: 15px;
      outline: none;
      transition: .28s ease;
    }
    .form-box input:focus,
    .form-box textarea:focus {
      border-color: #ff6b6b;
      box-shadow: 0 0 6px rgba(255, 95, 95, .45);
    }

    .form-box button {
      width: 100%;
      padding: 13px;
      border-radius: 7px;
      border: none;
      background: #ff6b6b;
      color: white;
      font-size: 17px;
      font-weight: 600;
      cursor: pointer;
      transition: .35s ease;
    }
    .form-box button:hover {
      background: #ff4b4b;
      box-shadow: 0 5px 18px rgba(255, 95, 95, .4);
      transform: translateY(-2px);
    }

    .alert {
      background: #ffdddd;
      color: #a10000;
      padding: 11px;
      margin-bottom: 15px;
      border-radius: 6px;
      text-align: center;
      font-weight: 500;
    }

    /* Footer */
    footer {
      background:#1e1e2f; color:#fff; padding:30px 5%; display:flex; flex-wrap:wrap; justify-content:space-between; gap:20px; margin-top:70px;
    }
    footer .footer-section { flex:1; min-width:200px; }
    footer h3 { color:#ff6b6b; margin-bottom:10px; }
    footer p, footer a { color:#fff; font-size:0.9rem; text-decoration:none; }
    footer a:hover { text-decoration:underline; }
    footer form input, footer form textarea { width:100%; padding:6px; margin-bottom:6px; border-radius:5px; border:1px solid #ccc; }
    footer button { background:#ff6b6b; color:#fff; border:none; padding:8px 12px; border-radius:5px; cursor:pointer; transition:0.3s; }
    footer button:hover { background:#ff4b4b; }

    /* Responsive */
    @media(max-width: 480px){
      header { flex-direction:column; align-items:flex-start; }
      header nav a { margin:5px; }
    }

    /* Navbar button animation */
header nav a {
  position: relative;
  padding: 8px 14px;
  color: white;
  font-weight: 500;
  transition: 0.3s ease;
}

header nav a::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -3px;
  width: 0;
  height: 2px;
  background: white;
  transition: 0.4s ease;
}

header nav a:hover::after {
  width: 100%;
}

header nav a:hover {
  transform: translateY(-2px);
  text-shadow: 0px 0px 8px rgba(255, 255, 255, 0.7);
}

  </style>
</head>
<body>

<header>
  <h1>BikeRental</h1>
  <nav>
    <a href="/">Home</a>
    <a href="/logout">Logout</a>
  </nav>
</header>

<main class="container">
  <div class="form-box">
    <h2><%= bike ? "Edit Bike" : "Add Bike" %></h2>

    <% if (message) { %>
      <div class="alert"><%= message %></div>
    <% } %>

   <form action="<%= bike ? '/admin/edit-bike/' + bike._id : '/admin/add-bike' %>" 
      method="POST" enctype="multipart/form-data" class="bike-form">

  <label>Name:</label>
  <input type="text" name="name" value="<%= bike ? bike.name : '' %>" required>

  <label>Price Per Day:</label>
  <input type="number" name="pricePerDay" value="<%= bike ? bike.pricePerDay : '' %>" required>

  <label>Description:</label>
  <textarea name="description" rows="3"><%= bike ? bike.description : '' %></textarea>

  <label>
    <input type="checkbox" name="isAvailable" <%= bike && bike.isAvailable ? 'checked' : '' %>> Available
  </label>

  <label>Image:</label>
  <% if (bike && bike.image) { %>
    <div class="preview-box">
      <img src="<%= bike.image %>" alt="Bike Image" class="preview-img">
    </div>
  <% } %>
  <input type="file" name="image">

  <button type="submit" class="submit-btn">
    <%= bike ? "Update Bike" : "Add Bike" %>
  </button>
</form>

  </div>
</main>

<footer>
  <div class="footer-section">
    <h3>BikeRental</h3>
    <p>Your trusted bike rental service.</p>
    <p>© <%= new Date().getFullYear() %> BikeRental</p>
  </div>
  <div class="footer-section">
    <h3>Links</h3>
    <p><a href="/">Home</a></p>
    <p><a href="/login">Login</a></p>
    <p><a href="/signup">Signup</a></p>
  </div>
  <div class="footer-section">
    <h3>Contact</h3>
    <p>Email: support@bikerental.com</p>
    <p>Phone: +91 9876543210</p>
    <p>Address: 123 Main St, City</p>
  </div>
  <div class="footer-section">
    <h3>Feedback</h3>
    <form action="/feedback" method="POST">
      <input type="text" name="name" placeholder="Your Name" required>
      <input type="email" name="email" placeholder="Email" required>
      <textarea name="message" placeholder="Your feedback" required></textarea>
      <button type="submit">Send</button>
    </form>
  </div>
</footer>

</body>
</html>
