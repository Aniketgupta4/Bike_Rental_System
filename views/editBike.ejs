<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Bike - Admin</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>

     * { margin:0; padding:0; box-sizing:border-box; font-family:'Poppins', sans-serif; }
    body { background:#f4f4f9; color:#333; line-height:1.6; }

    /* Header/Navbar */
    header {
      background:#1e1e2f; color:#fff; padding:15px 5%; display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap;
      box-shadow:0 2px 8px rgba(0,0,0,0.2);
    }
    header h1 { color:#ff6b6b; font-size:2rem; }
    header nav { display:flex; gap:15px; flex-wrap:wrap; }
    header nav a {
      color:#fff; text-decoration:none; padding:8px 16px; border-radius:6px; transition:0.3s; font-weight:500;
    }
    header nav a:hover { background:#ff6b6b; }

    /* Container */
    .container { width:90%; max-width:1200px; margin:auto; margin-top:20px; }

    h2 { margin:20px 0; color:#1e1e2f; text-align:center; }

    /* Buttons */
    .btn {
      display:inline-block; padding:6px 12px; border:none; border-radius:6px; color:#fff;
      background:#ff6b6b; cursor:pointer; transition:0.3s; text-decoration:none; font-weight:500; font-size:0.9rem;
    }
    .btn:hover { background:#ff4b4b; }
    .btn.small { padding:4px 8px; font-size:0.8rem; }
    .btn.danger { background:#e63946; }
    .btn.danger:hover { background:#c5303f; }

    /* Table */
    table { width:100%; border-collapse:collapse; margin-bottom:40px; }
    table th, table td { padding:12px 15px; border-bottom:1px solid #ddd; text-align:left; }
    table th { background:#ff6b6b; color:#fff; font-weight:600; }
    table tr:hover { background:#f1f1f1; }
    .small-note { font-size:0.8rem; color:#777; }

    /* Cards */
    .card { background:#fff; padding:15px; border-radius:10px; box-shadow:0 2px 6px rgba(0,0,0,0.1); margin:10px 0; text-align:center; }

    /* Footer */
    footer {
      background:#1e1e2f; color:#fff; padding:30px 5%; display:flex; flex-wrap:wrap; justify-content:space-between;
      gap:20px;
    }
    footer .footer-section { flex:1; min-width:200px; }
    footer h3 { color:#ff6b6b; margin-bottom:10px; }
    footer p, footer a { color:#fff; font-size:0.9rem; text-decoration:none; }
    footer a:hover { text-decoration:underline; }

    /* Responsive */
    @media (max-width:768px){
      table, thead, tbody, th, td, tr { display:block; }
      table tr { margin-bottom:15px; }
      table td { padding-left:50%; position:relative; text-align:left; }
      table td::before {
        position:absolute; top:12px; left:12px; width:45%; white-space:nowrap; font-weight:bold;
      }
      table td:nth-of-type(1)::before { content:"User/Bike"; }
      table td:nth-of-type(2)::before { content:"Bike/Price"; }
      table td:nth-of-type(3)::before { content:"Days/Available"; }
      table td:nth-of-type(4)::before { content:"Total/Actions"; }
      table td:nth-of-type(5)::before { content:"Status"; }
      table td:nth-of-type(6)::before { content:"Action"; }
    }
    /* Reset & basic styles */
    * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; }
    body { background: #f5f5f8; color: #111; }

    /* Header */
    header {
      background: #1b1b2f;
      color: #ff6b6b;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 2rem;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    header .brand { font-size: 1.5rem; font-weight: 700; }
    header nav a {
      color: #fff;
      text-decoration: none;
      margin-left: 1.5rem;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      transition: all 0.3s ease;
    }
    header nav a:hover {
      background: #ff6b6b;
      color: #fff;
      transform: translateY(-2px);
    }

    /* Container */
    .container {
      max-width: 500px;
      margin: 4rem auto;
      padding: 2rem;
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }
    .container h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #1b1b2f;
    }

    /* Form */
    .form-box input, .form-box textarea {
      width: 100%;
      padding: 0.8rem;
      margin-bottom: 1rem;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-size: 1rem;
      transition: all 0.3s ease;
    }
    .form-box input:focus, .form-box textarea:focus {
      border-color: #ff6b6b;
      outline: none;
      box-shadow: 0 0 5px rgba(255,107,107,0.5);
    }
    .form-box label {
      display: flex;
      align-items: center;
      margin-bottom: 1rem;
      font-weight: 500;
    }
    .form-box label input {
      margin-right: 0.5rem;
      width: auto;
    }

    /* Button */
    .btn {
      width: 100%;
      padding: 0.8rem;
      background: #ff6b6b;
      color: #fff;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    .btn:hover {
      background: #ff4c4c;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(255, 107, 107, 0.3);
    }

    /* Footer */
    footer {
      text-align: center;
      padding: 2rem;
      background: #1b1b2f;
      color: #ff6b6b;
      margin-top: 4rem;
    }
  </style>
</head>
<body>

<header>
  <div class="brand">BikeRental</div>
  <nav>
    <a href="/admin/dashboard">Back</a>
    <a href="/logout">Logout</a>
  </nav>
</header>

<div class="container">
  <h2>Edit Bike</h2>
     <form class="form-box" action="/admin/edit-bike/<%= bike._id %>" method="POST" enctype="multipart/form-data">

      <label>Bike Name</label>
      <input type="text" name="name" value="<%= bike.name %>" required>

      <label>Price Per Day (₹)</label>
      <input type="number" name="pricePerDay" value="<%= bike.pricePerDay %>" required>

      <label>Description</label>
      <textarea name="description" rows="4" required><%= bike.description %></textarea>

      <label class="checkbox-label">
        <input type="checkbox" name="isAvailable" <%= bike.isAvailable ? 'checked' : '' %> > Available
      </label>

      <label>Bike Image</label>
      <input type="file" name="image" accept="image/*">

      <% if (bike.image) { %>
        <div class="image-preview">
          <img src="/uploads/<%= bike.image %>" alt="Bike Image">
        </div>
      <% } %>

      <button class="btn" type="submit">Save Changes</button>
    </form>
</div>

 <footer>
    <div class="footer-section">
      <h3>BikeRental</h3>
      <p>Your trusted bike rental service.</p>
      <p>© <%= new Date().getFullYear() %> BikeRental</p>
    </div>
    <div class="footer-section">
      <h3>Links</h3>
      <p><a href="/">Home</a></p>
      <p><a href="/login">Login</a></p>
      <p><a href="/signup">Signup</a></p>
    </div>
    <div class="footer-section">
      <h3>Contact</h3>
      <p>Email: support@bikerental.com</p>
      <p>Phone: +91 9876543210</p>
      <p>Address: 123 Main St, City</p>
    </div>
    <div class="footer-section">
      <h3>Feedback</h3>
      <form action="/feedback" method="POST">
        <input type="text" name="name" placeholder="Your Name" required style="width:100%; margin-bottom:5px; padding:5px;">
        <input type="email" name="email" placeholder="Email" required style="width:100%; margin-bottom:5px; padding:5px;">
        <textarea name="message" placeholder="Your feedback" required style="width:100%; padding:5px;"></textarea>
        <button type="submit" class="btn" style="margin-top:5px;">Send</button>
      </form>
    </div>
  </footer>

</body>
</html>
